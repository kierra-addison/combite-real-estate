<div class="row">
  <% @posts.each do |post| %>
    <div class="col-sm-6 col-md-4">
      <%= link_to (visit_post_url(post)) do %>
        <div class="thumbnail">
          <% if post.title_image.attached? %>
            <%= image_tag post.title_image %>
          <% end %>

          <div class="caption">
            <% post.post_attributes.first(2).each do |attribute| %>
              <div>
                <%= attribute.attribute_name %>:<strong> <%= attribute.attribute_value %></strong>
              </div>
            <% end %>
            <h3><%= post.title %></h3>
            <p><%= truncate(post.description, length: 100) %></p>
          </div>
        </div>
      <% end %>
    </div>
  <% end %>
</div>
