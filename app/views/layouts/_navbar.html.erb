<nav class="navbar navbar-default navbar-static-top">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" 
                            data-toggle="collapse" 
                            data-target="#bs-example-navbar-collapse-1" 
                            aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to image_tag("https://s3.amazonaws.com/combite-real-estate/public/rmres-logo.png", 
                            alt: "RM Real Estate Services"), root_url, class: "navbar-brand" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" 
                                              role="button" aria-haspopup="true" 
                                              aria-expanded="false"><strong>Categories</strong><span 
                                              class="caret"></span></a>
          <ul class="dropdown-menu">
            <% if categories.any? %>
              <% categories.each do |category| %>
                <li><%= link_to category.category_name, category %></li>
              <% end %>
            <% else %>
              <li><a href="#">No category found</a></li>
            <% end %>
          </ul>
        </li>
        <li><%= link_to "Contact us", root_url, class: "nav-bar-link" %></li>
        <li><%= link_to "About", root_url, class: "nav-bar-link" %></li>
        <% if current_user %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" 
                                                role="button" aria-haspopup="true" 
                                                aria-expanded="false"><strong>Actions</strong><span 
                                                class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><%= link_to "New Post", new_post_url %></li>
              <li><%= link_to "Manage Posts", posts_url %></li>
              <li role="separator" class="divider"></li>
              <li><%= link_to "New Category", new_category_url %></li>
              <li><%= link_to "Manage Categories", categories_url %></li>
              <li role="separator" class="divider"></li>
              <li><%= link_to "Manage Users", users_url %></li>
            </ul>
          </li>
          <li><a>Signed in as: <strong><%= current_user.username %></strong></a></li> 
          <li><%= link_to "Log out", logout_url, method: "delete", class: "" %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>